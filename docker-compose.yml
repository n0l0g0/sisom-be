version: "2.2"
services:
  backend:
    build: .
    container_name: sisom-backend
    ports:
      - "3010:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - /root/uploads:/app/uploads
    restart: unless-stopped
